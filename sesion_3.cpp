#include <iostream>

int main() {
    
    const double PI = 3.14159 ; // We can declare a constant variable with the const keyword
                                // also we need to the name of the variable in capital letters
                                // with this we can't change the value of the variable
    //PI = 3.14; // This will give us an error because we can't change the value of a constant variable


    return 0;
}